---
# Running the 5GTANGO Security Gateway (Docker container)
- debug: msg="##### 5GTANGO SEC-GTW DEPLOYMENT {{ lookup('pipe', 'date +%Y%m%d-%H%M') }}"
- name: run the 5GTANGO Security Gateway
  docker_container:
    name: "son-sec-gw"
    image: "{{ repository }}/son-sec-gw:{{ version }}"
    state: started
    recreate: yes
    pull: true
    restart_policy: "always"
    network_mode: bridge
    networks:
      - name: "{{ docker_network_name }}"
        aliases:
          - son-sec-gw
    published_ports:
      - "80:80"
      - "443:443"
    volumes: 
      - "/etc/ssl/private/sonata/:/etc/nginx/cert/"


---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: win-wrapper-mock
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: win-wrapper-mock
        platform: sonata
    spec:
      containers:
      - name: win-wrapper-mock
        image: sonatanfv/win-wrapper-mock:dev
        imagePullPolicy: IfNotPresent
        env:
        - name: broker_host
          value: "son-broker"
        - name: broker_uri
          value: "amqp://guest:guest@son-broker:5672/%2F"
        - name: repo_host
          value: "son-postgres"
        - name: repo_port
          value: "5432"
        - name: repo_user
          value: "{{ ia_repo_user }}"
        - name: repo_pass
          value: "{{ ia_repo_pass }}"
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: vim-wrapper-ovs
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: vim-wrapper-ovs
        platform: sonata
    spec:
      containers:
      - name: vim-wrapper-ovs
        image: sonatanfv/vim-wrapper-ovs:dev
        imagePullPolicy: IfNotPresent
        env:
        - name: broker_host
          value: "son-broker"
        - name: broker_uri
          value: "amqp://guest:guest@son-broker:5672/%2F"
        - name: repo_host
          value: "son-postgres"
        - name: repo_port
          value: "5432"
        - name: repo_user
          value: "{{ ia_repo_user }}"
        - name: repo_pass
          value: "{{ ia_repo_pass }}"
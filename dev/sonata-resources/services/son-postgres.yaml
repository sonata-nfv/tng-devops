---
apiVersion: v1
kind: Service
metadata:
  name: son-postgres
  labels:
    app: son-postgres
    platform: sonata
    version: "5"
spec:
  ports:
  - port: 5432
    name: tcp-postgres
    targetPort: 5432
  selector:
    app: son-postgres
  type: ClusterIP
